[project]
name = "django-class"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.14",
    "pandas>=3.0.3",
]

[tool.uv.workspace]
members = [
    "Django practice",
]
